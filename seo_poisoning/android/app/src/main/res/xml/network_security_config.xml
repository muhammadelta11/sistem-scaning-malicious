<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- 
        Production Configuration:
        Hanya izinkan HTTPS untuk production
        Uncomment bagian ini dan comment development config saat build untuk Play Store
    -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
            <!-- Uncomment untuk custom CA jika perlu -->
            <!-- <certificates src="user" /> -->
        </trust-anchors>
    </base-config>

    <!-- 
        Development Configuration:
        Izin HTTP untuk development/testing
        Comment bagian ini saat build untuk Play Store
    -->
    <!--
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </base-config>
    
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.211.55.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">192.168.1.0</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
    </domain-config>
    -->
</network-security-config>

